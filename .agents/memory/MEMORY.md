- [Rotation resolve-loop invariant](rotation-resolve-loop-invariant.md) — DJBooth name→track save loops must keep unresolved names as name-only entries, never drop them (set-shortening bug).
- [Update rollback data loss](update-rollback-data-loss.md) — a 300GB R2 boot re-download is a SYMPTOM: failed-update rollback wiped local music (R2 sync is incremental/innocent); rollback must carry backup-EXCLUDED dirs across fail-closed; stale-script units re-wipe every failure.
- [Feature placement is circular](feature-placement-circular.md) — feature slot placement is by 1-based PLAY POSITION on a circular rotation (re-seat current to idx 0); panel + booth must share the same model or "Next on stage" lies.
- [GitHub push operational](github-push-operational.md) — PUBLIC repo: push only changed files+dist (full-tree trips secondary rate limit); harden ignores beyond skill defaults (music/, license-keys.json secret, reports/).
- [Dell update scripts](dell-update-script-paths.md) — unit's ~/djbooth-update.sh IS the github updater (not the repo's cloud-pulling djbooth-update.sh); skip homebase with DJBOOTH_SKIP_HOMEBASE=1.
- [api-server live tree](api-server-active-tree.md) — edit server/ (live for dev + units); src/+dist build targets the dead Replit deploy. feature-producer.js only in server/.
- [Featured Beds genre](featured-beds-genre.md) — feature intro/outro beds come from a top-level music genre "Featured Beds" (folder=genre); DECIDED, not built — held until user creates+fills the folder.
- [Stale self-updating updater](stale-self-updating-updater.md) — a unit on disabled auto-update keeps an OLD ~/djbooth-update.sh that can't self-replace; re-running fails identically — manually curl the GitHub-raw script over it to break the loop.
- [Deactivate = replace-in-place](deactivate-replace-in-place.md) — deactivate must swap a FRESH song into the same slot, never count against the entertainer, never end set early; booth + remote share one helper.
- [Config precedence + clobber](config-precedence-and-clobber.md) — stored UI value WINS over env (env only seeds); always-mounted components save ONLY what they edit; voice ID is per-unit, not fleet-synced.
- [Voice switch on a unit](voice-switch-elevenlabs.md) — change voice ID in Options (per-unit, no deploy); silence cause = voice not v3-enabled / not in My Voices; cache key omits voiceID so Clear All Voiceovers to flip + it propagates to fleet via R2.
- [002 touch recovery](touch-recovery-002.md) — 002's real panel is Siliconworks SiW (not the Weida replit.md claims); always `xinput list` for the live id; after monitor power-cycle: Rotation Screen button then `xinput map-to-output <id> HDMI-2` to fix offset.
- [Fleet desktop WM](fleet-desktop-wm.md) — both 002 AND 003 run Openbox now (GNOME removed from 003, freeze fix DONE); gnome-shell grabs X input on gestures = the freeze cause; verify with `wmctrl -m`.
- [LOW CACHE RATE alert](low-cache-rate-alert.md) — "pre-pick misses" Telegram alert is a cold-cache-after-reboot artifact, NOT a music wipe; self-heals; real wipe = DEAD AIR + R2 re-download.
- [replit.md split](replit-md-archive-split.md) — replit.md trimmed Jun 07 2026; full history/TODO forensics moved VERBATIM to replit-archive.md (committed). Read both; nothing lost.
- [SiW touch autosuspend rule mismatch](siw-touch-autosuspend-rule.md) — 003's live SiW enumerates as USB 1fd2:b101 but 97-djbooth-touch-power.rules pins 9101 so it never matches; touch drops = USB re-enumeration, watchdog recovers ~3min, control already on so autosuspend isn't the cause.
